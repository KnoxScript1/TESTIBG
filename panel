--==================================================
-- KNOX PLAYER CONTROLS
-- ONLY THE CONTROL PANEL REMAINS AFTER VERIFICATION
--==================================================

local controlGui = Instance.new("ScreenGui")
controlGui.Name = "KnoxControls"
controlGui.ResetOnSpawn = false
controlGui.IgnoreGuiInset = true
controlGui.DisplayOrder = 100
controlGui.Parent = player:WaitForChild("PlayerGui")

--==================================================
-- MAIN PANEL
--==================================================

local panel = Instance.new("Frame")
panel.Size = UDim2.fromOffset(260, 180)
panel.Position = UDim2.new(1, -280, 0.5, -90)
panel.BackgroundColor3 = Color3.fromRGB(10, 8, 20)
panel.BackgroundTransparency = 0.08
panel.BorderSizePixel = 0
panel.Active = true
panel.Parent = controlGui

local panelCorner = Instance.new("UICorner")
panelCorner.CornerRadius = UDim.new(0, 16)
panelCorner.Parent = panel

local panelStroke = Instance.new("UIStroke")
panelStroke.Color = Color3.fromRGB(110, 70, 220)
panelStroke.Thickness = 1.5
panelStroke.Transparency = 0.15
panelStroke.Parent = panel

--==================================================
-- DRAGGABLE PANEL
--==================================================

local dragging = false
local dragStart
local startPosition

panel.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPosition = panel.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

panel.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then

		dragInput = input
	end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
	if dragging and input == dragInput then

		local delta = input.Position - dragStart

		panel.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)

	end
end)

--==================================================
-- TITLE
--==================================================

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -20, 0, 32)
title.Position = UDim2.fromOffset(10, 8)
title.BackgroundTransparency = 1
title.Text = "KNOX CONTROLS"
title.TextColor3 = Color3.fromRGB(220, 200, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.Parent = panel

--==================================================
-- SPEED LABEL
--==================================================

local speedLabel = Instance.new("TextLabel")
speedLabel.Size = UDim2.new(0.5, -10, 0, 25)
speedLabel.Position = UDim2.fromOffset(12, 48)
speedLabel.BackgroundTransparency = 1
speedLabel.Text = "Speed: 16"
speedLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
speedLabel.Font = Enum.Font.GothamMedium
speedLabel.TextSize = 14
speedLabel.TextXAlignment = Enum.TextXAlignment.Left
speedLabel.Parent = panel

--==================================================
-- SPEED SLIDER
--==================================================

local speedBar = Instance.new("Frame")
speedBar.Size = UDim2.new(1, -24, 0, 8)
speedBar.Position = UDim2.fromOffset(12, 75)
speedBar.BackgroundColor3 = Color3.fromRGB(35, 30, 55)
speedBar.BorderSizePixel = 0
speedBar.Parent = panel

local speedCorner = Instance.new("UICorner")
speedCorner.CornerRadius = UDim.new(1, 0)
speedCorner.Parent = speedBar

local speedFill = Instance.new("Frame")
speedFill.Size = UDim2.fromScale(16 / 100, 1)
speedFill.BackgroundColor3 = Color3.fromRGB(130, 80, 255)
speedFill.BorderSizePixel = 0
speedFill.Parent = speedBar

local speedFillCorner = Instance.new("UICorner")
speedFillCorner.CornerRadius = UDim.new(1, 0)
speedFillCorner.Parent = speedFill

--==================================================
-- JUMP LABEL
--==================================================

local jumpLabel = Instance.new("TextLabel")
jumpLabel.Size = UDim2.new(0.5, -10, 0, 25)
jumpLabel.Position = UDim2.fromOffset(12, 96)
jumpLabel.BackgroundTransparency = 1
jumpLabel.Text = "Jump: 50"
jumpLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
jumpLabel.Font = Enum.Font.GothamMedium
jumpLabel.TextSize = 14
jumpLabel.TextXAlignment = Enum.TextXAlignment.Left
jumpLabel.Parent = panel

--==================================================
-- JUMP SLIDER
--==================================================

local jumpBar = Instance.new("Frame")
jumpBar.Size = UDim2.new(1, -24, 0, 8)
jumpBar.Position = UDim2.fromOffset(12, 123)
jumpBar.BackgroundColor3 = Color3.fromRGB(35, 30, 55)
jumpBar.BorderSizePixel = 0
jumpBar.Parent = panel

local jumpCorner = Instance.new("UICorner")
jumpCorner.CornerRadius = UDim.new(1, 0)
jumpCorner.Parent = jumpBar

local jumpFill = Instance.new("Frame")
jumpFill.Size = UDim2.fromScale(50 / 150, 1)
jumpFill.BackgroundColor3 = Color3.fromRGB(70, 130, 255)
jumpFill.BorderSizePixel = 0
jumpFill.Parent = jumpBar

local jumpFillCorner = Instance.new("UICorner")
jumpFillCorner.CornerRadius = UDim.new(1, 0)
jumpFillCorner.Parent = jumpFill

--==================================================
-- PLAYER VALUES
--==================================================

local speedValue = 16
local jumpValue = 50

local function updateCharacter()
	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	humanoid.WalkSpeed = speedValue
	humanoid.UseJumpPower = true
	humanoid.JumpPower = jumpValue
end

player.CharacterAdded:Connect(function()
	task.wait(0.5)
	updateCharacter()
end)

--==================================================
-- SLIDER FUNCTION
--==================================================

local UserInputService = game:GetService("UserInputService")

local function createSlider(bar, fill, minValue, maxValue, callback)

	local draggingSlider = false

	local function setValue(x)

		local relative = math.clamp(
			(x - bar.AbsolutePosition.X) / bar.AbsoluteSize.X,
			0,
			1
		)

		local value = math.floor(
			minValue + (maxValue - minValue) * relative
		)

		fill.Size = UDim2.fromScale(relative, 1)

		callback(value)
	end

	bar.InputBegan:Connect(function(input)

		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			draggingSlider = true
			setValue(input.Position.X)
		end

	end)

	UserInputService.InputChanged:Connect(function(input)

		if draggingSlider then

			if input.UserInputType == Enum.UserInputType.MouseMovement
				or input.UserInputType == Enum.UserInputType.Touch then

				setValue(input.Position.X)
			end

		end

	end)

	UserInputService.InputEnded:Connect(function(input)

		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			draggingSlider = false
		end

	end)

end

--==================================================
-- SPEED CONTROL
--==================================================

createSlider(
	speedBar,
	speedFill,
	16,
	100,
	function(value)

		speedValue = value

		speedLabel.Text = "Speed: " .. value

		updateCharacter()

	end
)

--==================================================
-- JUMP CONTROL
--==================================================

createSlider(
	jumpBar,
	jumpFill,
	50,
	150,
	function(value)

		jumpValue = value

		jumpLabel.Text = "Jump: " .. value

		updateCharacter()

	end
)

--==================================================
-- INITIALIZE
--==================================================

updateCharacter()
